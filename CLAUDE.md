# Bash Commands
- Always run python or python script in the env `agent_venv`: `source ~/agent_venv/bin/activate`

# Important Folders
- Claude's Local Skill folder: `~/.claude/skills` 

# Rules
## Sync Claude Skill
- only sync `SKILL.md` and folder `scripts`